#include <iostream>

int main()
{
	/*PEMDAS rule of precedence indicates the importance given to operations by the machine
    P ------ Paranthasis
    E ------ Exponent
    MD ------ Multiplication & Division
    AS ------ Addition & Substraction
    */
    
    /*
    There're also compound assignments such as += *= %= >>= &= ^= |=
    Let's see what += means: num1 += num2 ---> num1 = num1 + num2; Very eficient
    Another example: A *= B + C ---> A = A * (B + C);
    
    */
	return 0;
}
