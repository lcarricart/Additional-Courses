#include <iostream>

using namespace std;

/*If I run the debugger I have to press the arrwos to go through the lines.
Selecting 'Watches I can evaluate expressions inside a 'for' conditional like (i < 0). */

int main()
{
	
    
	return 0;
}
